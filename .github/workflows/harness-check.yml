name: Harness Constraint Check

on:
  push:
    paths:
      - 'src/**/*.py'
      - 'harness/**'
      - 'tests/**/*.py'
      - 'pyproject.toml'
      - '.github/workflows/harness-check.yml'
  pull_request:

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -e ".[test]"

      - name: Run harness rules check (dry-run with sample)
        run: |
          python scripts/check_harness.py --rules

      - name: Run unit tests
        run: python -m pytest tests/test_lightweight_mcp.py tests/test_tool_profiles.py tests/test_auth_mapping.py tests/test_limits.py -q
