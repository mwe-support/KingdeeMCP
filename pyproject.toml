[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "kingdee-mcp"
version = "0.1.0"
description = "MCP Server for Kingdee Cloud K/3 - Works with any MCP-compatible AI client"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
authors = [
    { name = "WaHaiLong" }
]
keywords = ["mcp", "kingdee", "erp", "ai", "k3cloud", "claude", "cursor", "windsurf", "cline"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Topic :: Office/Business :: Financial :: Accounting",
]
dependencies = [
    "httpx>=0.27.0",
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "pytest-asyncio>=0.23.0",
]

[project.urls]
Homepage = "https://github.com/mwe-support/KingdeeMCP"
Repository = "https://github.com/mwe-support/KingdeeMCP"
Documentation = "https://github.com/mwe-support/KingdeeMCP#readme"

[project.scripts]
kingdee-mcp = "kingdee_mcp.main:main"
kingdee-mcp-token = "kingdee_mcp.token_cli:main"

[tool.hatch.build.targets.wheel]
packages = ["src/kingdee_mcp"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
    "e2e: 真实金蝶环境的端到端测试（需 KINGDEE_* 环境变量）",
]
